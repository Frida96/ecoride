{% extends 'base.html.twig' %}

{% block title %}Mon Profil - EcoRide{% endblock %}

{% block body %}
<div class="container my-5">
    <div class="row">
        <!-- Header profil -->
        <div class="col-12 mb-4">
            <div class="card">
                <div class="card-body p-4">
                    <div class="row align-items-center">
                        <div class="col-md-6">
                            <div class="d-flex align-items-center">
                                <div class="avatar-large me-4">
                                    <i class="fas fa-user"></i>
                                </div>
                                <div>
                                    <h2 class="fw-bold mb-1">{{ user.pseudo }}</h2>
                                    <p class="text-muted mb-2">{{ user.email }}</p>
                                    <span class="badge bg-{{ user.role == 'passager' ? 'primary' : (user.role == 'chauffeur' ? 'success' : 'warning') }} fs-6">
                                        <i class="fas fa-{{ user.role == 'passager' ? 'user' : (user.role == 'chauffeur' ? 'car' : 'users') }} me-1"></i>
                                        {{ user.role|replace({'_': ' et '})|title }}
                                    </span>
                                </div>
                            </div>
                        </div>
                        <div class="col-md-6 text-md-end">
                            <div class="credit-display">
                                <div class="h3 text-success mb-0">
                                    <i class="fas fa-coins me-2"></i>{{ user.credit }} crédits
                                </div>
                                <small class="text-muted">Solde disponible</small>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Sélection du rôle -->
        <div class="col-lg-4 mb-4">
            <div class="card h-100">
                <div class="card-body p-4">
                    <h4 class="fw-bold mb-3">
                        <i class="fas fa-user-cog text-primary me-2"></i>
                        Mon rôle
                    </h4>
                    
                    <p class="text-muted mb-4">
                        Choisissez votre mode d'utilisation d'EcoRide
                    </p>

                    <div class="role-options">
                        <div class="role-card {{ user.role == 'passager' ? 'active' : '' }} mb-3">
                            <div class="p-3 border rounded">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <h6 class="fw-bold mb-1">
                                            <i class="fas fa-user text-primary me-2"></i>Passager
                                        </h6>
                                        <small class="text-muted">Je recherche des trajets</small>
                                    </div>
                                    {% if user.role != 'passager' %}
                                        <a href="{{ path('app_profil_changer_role', {role: 'passager'}) }}" 
                                           class="btn btn-sm btn-outline-primary">
                                            Choisir
                                        </a>
                                    {% else %}
                                        <span class="badge bg-primary">Actuel</span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>

                        <div class="role-card {{ user.role == 'chauffeur' ? 'active' : '' }} mb-3">
                            <div class="p-3 border rounded">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <h6 class="fw-bold mb-1">
                                            <i class="fas fa-car text-success me-2"></i>Chauffeur
                                        </h6>
                                        <small class="text-muted">Je propose des trajets</small>
                                    </div>
                                    {% if user.role != 'chauffeur' %}
                                        <a href="{{ path('app_profil_changer_role', {role: 'chauffeur'}) }}" 
                                           class="btn btn-sm btn-outline-success">
                                            Choisir
                                        </a>
                                    {% else %}
                                        <span class="badge bg-success">Actuel</span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>

                        <div class="role-card {{ user.role == 'passager_chauffeur' ? 'active' : '' }}">
                            <div class="p-3 border rounded">
                                <div class="d-flex justify-content-between align-items-center">
                                    <div>
                                        <h6 class="fw-bold mb-1">
                                            <i class="fas fa-users text-warning me-2"></i>Les deux
                                        </h6>
                                        <small class="text-muted">Passager ET chauffeur</small>
                                    </div>
                                    {% if user.role != 'passager_chauffeur' %}
                                        <a href="{{ path('app_profil_changer_role', {role: 'passager_chauffeur'}) }}" 
                                           class="btn btn-sm btn-outline-warning">
                                            Choisir
                                        </a>
                                    {% else %}
                                        <span class="badge bg-warning text-dark">Actuel</span>
                                    {% endif %}
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Actions chauffeur -->
        {% if user.role in ['chauffeur', 'passager_chauffeur'] %}
            <div class="col-lg-8">
                <div class="row g-4">
                    <!-- Mes véhicules -->
                    <div class="col-md-6">
                        <div class="card h-100">
                            <div class="card-body p-4">
                                <h5 class="fw-bold mb-3">
                                    <i class="fas fa-car text-success me-2"></i>
                                    Mes véhicules
                                </h5>
                                
                                {% if user.vehicles|length > 0 %}
                                    <p class="text-muted mb-3">{{ user.vehicles|length }} véhicule{{ user.vehicles|length > 1 ? 's' : '' }} enregistré{{ user.vehicles|length > 1 ? 's' : '' }}</p>
                                    
                                    <div class="vehicle-list mb-3">
                                        {% for vehicle in user.vehicles|slice(0, 2) %}
                                            <div class="d-flex align-items-center mb-2">
                                                <div class="vehicle-icon me-2">
                                                    {% if vehicle.energie == 'electrique' %}
                                                        <i class="fas fa-leaf text-success"></i>
                                                    {% else %}
                                                        <i class="fas fa-car text-muted"></i>
                                                    {% endif %}
                                                </div>
                                                <div>
                                                    <strong>{{ vehicle.marque }} {{ vehicle.modele }}</strong>
                                                    <small class="text-muted d-block">{{ vehicle.couleur }} - {{ vehicle.energie|title }}</small>
                                                </div>
                                            </div>
                                        {% endfor %}
                                        {% if user.vehicles|length > 2 %}
                                            <small class="text-muted">Et {{ user.vehicles|length - 2 }} autre{{ user.vehicles|length > 3 ? 's' : '' }}...</small>
                                        {% endif %}
                                    </div>
                                {% else %}
                                    <div class="alert alert-warning">
                                        <i class="fas fa-exclamation-triangle me-2"></i>
                                        <strong>Aucun véhicule</strong><br>
                                        <small>Ajoutez vos véhicules pour proposer des trajets</small>
                                    </div>
                                {% endif %}

                                <a href="{{ path('app_profil_vehicules') }}" class="btn btn-success w-100">
                                    <i class="fas fa-cog me-2"></i>Gérer mes véhicules
                                </a>
                            </div>
                        </div>
                    </div>

                    <!-- Préférences -->
                    <div class="col-md-6">
                        <div class="card h-100">
                            <div class="card-body p-4">
                                <h5 class="fw-bold mb-3">
                                    <i class="fas fa-heart text-primary me-2"></i>
                                    Mes préférences
                                </h5>
                                
                                {% if user.preferences %}
                                    <div class="preferences-preview mb-3">
                                        {% set preferences = user.preferences|split(', ') %}
                                        {% for preference in preferences|slice(0, 3) %}
                                            {% set parts = preference|split(': ') %}
                                            <div class="d-flex justify-content-between mb-1">
                                                <span class="text-muted">{{ parts[0] }}:</span>
                                                <strong class="{{ parts[1] == 'Oui' ? 'text-success' : 'text-danger' }}">
                                                    {{ parts[1] }}
                                                </strong>
                                            </div>
                                        {% endfor %}
                                        {% if preferences|length > 3 %}
                                            <small class="text-muted">Et plus...</small>
                                        {% endif %}
                                    </div>
                                {% else %}
                                    <div class="alert alert-info">
                                        <i class="fas fa-info-circle me-2"></i>
                                        <strong>Préférences non définies</strong><br>
                                        <small>Configurez vos préférences de voyage</small>
                                    </div>
                                {% endif %}

                                <a href="{{ path('app_profil_preferences') }}" class="btn btn-primary w-100">
                                    <i class="fas fa-edit me-2"></i>Configurer
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        {% else %}
            <!-- Message pour les passagers -->
            <div class="col-lg-8">
                <div class="card">
                    <div class="card-body p-4 text-center">
                        <i class="fas fa-user-friends text-primary" style="font-size: 4rem; margin-bottom: 20px;"></i>
                        <h4 class="fw-bold mb-3">Mode Passager</h4>
                        <p class="lead mb-4">
                            Vous êtes actuellement en mode passager. Vous pouvez rechercher et 
                            participer aux covoiturages proposés par notre communauté.
                        </p>
                        <div class="row g-3">
                            <div class="col-md-6">
                                <a href="{{ path('app_covoiturages') }}" class="btn btn-primary w-100">
                                    <i class="fas fa-search me-2"></i>Rechercher un trajet
                                </a>
                            </div>
                            <div class="col-md-6">
                                <button class="btn btn-outline-secondary w-100">
                                    <i class="fas fa-history me-2"></i>Mes participations
                                </button>
                            </div>
                        </div>
                        <hr class="my-4">
                        <p class="text-muted">
                            Envie de proposer vos propres trajets ? 
                            <strong>Devenez chauffeur</strong> pour partager vos véhicules !
                        </p>
                    </div>
                </div>
            </div>
        {% endif %}
    </div>
</div>

<style>
.avatar-large {
    width: 80px;
    height: 80px;
    background: linear-gradient(45deg, var(--eco-green), var(--eco-blue));
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    color: white;
    font-size: 2rem;
}

.role-card.active {
    border-color: var(--eco-green) !important;
    background: rgba(34, 197, 94, 0.05);
}

.credit-display {
    background: rgba(34, 197, 94, 0.1);
    padding: 20px;
    border-radius: 10px;
    text-align: center;
}

.vehicle-icon {
    width: 30px;
    text-align: center;
}

.preferences-preview {
    background: rgba(0, 0, 0, 0.05);
    padding: 15px;
    border-radius: 8px;
}
</style>
{% endblock %}
